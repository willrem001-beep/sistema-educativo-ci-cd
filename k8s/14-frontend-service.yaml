apiVersion: v1
kind: Service
metadata:
  name: ms-frontend-service
spec:
  type: NodePort
  selector:
    app: ms-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30005