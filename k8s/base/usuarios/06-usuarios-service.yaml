apiVersion: v1
kind: Service
metadata:
  name: ms-usuarios
  labels:
    app: ms-usuarios
spec:
  type: ClusterIP
  selector:
    app: ms-usuarios
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001